{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
          //  "command": "g++ -I./includes -L /lib -c *.cpp && g++ -I./includes -L  \"./lib/libIrrKlang.so\" *.o -o a.out -lGL -lGLU -lglut -IrrKlang"
            //"command": "g++ -c *.cpp && g++  *.o -o a.out -lGL  -lGLU -lglut"

            "command": "g++ -I./includes -L ~/lib -c *.cpp && g++ -I./includes -L  \"./lib/libIrrKlang.so\" *.o -o a.out -lGL -lGLU -lglut -IrrKlang"
        }
        
    ]
}

